name: Fetch and process ruleset snapshots

on:
  schedule:
    - cron: "0 18 * * *"  # 21:00 UTC = 05:00 AM UTC+8 (next day)  "I changed  it to 18:00 UTC".
  workflow_dispatch:

permissions:
  contents: write

jobs:
  fetch:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          persist-credentials: true

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.x"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Fetch ruleset data
        run: |
          python fetch_ruleset.py

      - name: Post-process and curate snapshot files
        run: |
          python process_ruleset.py

      - name: Configure git user
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: Commit changes if any
        run: |
          git status
          if [ -n "$(git status --porcelain)" ]; then
            git add data
            git commit -m "Fetch and curate ruleset snapshot for $(date -u +'%Y-%m-%d')"
            git push origin HEAD:${GITHUB_REF_NAME}
          else
            echo "No changes to commit."
          fi
